<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity$PlaceholderFragment">

    <Button
        android:id="@+id/blue_on_dialog"
        android:text="Bluetooth ON Dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/blue_on"
        android:layout_below="@id/blue_on_dialog"
        android:text="Bluetooth ON"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/blue_off"
        android:layout_below="@id/blue_on"
        android:text="Bluetooth OFF"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/blue_discover"
        android:layout_below="@id/blue_off"
        android:text="Bluetooth DISCOVER ABILITY"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/blue_find"
        android:layout_below="@id/blue_discover"
        android:text="Bluetooth FIND"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/blue_list"
        android:layout_below="@id/blue_find"
        android:layout_height="match_parent"
        android:layout_width="match_parent"></ListView>

</RelativeLayout>
